version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jubjub_app
    ports:
      - "5001:5001"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    command: npm run dev
